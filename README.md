# Egyptian Fractions

This repository contains code to compute **Egyptian fraction representations** of positive rational numbers using the **greedy algorithm**.

An **Egyptian fraction** expresses a fraction as a sum of distinct unit fractions of the form `1/n`.
